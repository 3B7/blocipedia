<h5><%= markdown @article.title %></h5>

<div class="row">
  <div class="span8">
    <p><%= markdown @article.body %></p>
  </div>
  <div class="span4">
    <% if can? :edit, @article %>
  <%= link_to "Edit", edit_wiki_article_path(@wiki, @article), class: 'btn btn-mini' %>
    <% end %>
  </div>
</div>